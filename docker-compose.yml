version: '3'

services:
  postgres:
    image: postgres:17.5-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=rie
    ports:
      - "5432:5432"

volumes:
  postgres-data:
